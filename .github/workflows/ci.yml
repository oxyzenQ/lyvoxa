# =============================================================================
# LYVOXA CI PIPELINE - PROFESSIONAL ORDERED BUILD
# =============================================================================
# Strict ordering: CodeQL → DCO → Build/Test → Performance
# Normal mode (push to main): All steps run
# Release mode (tag): CI runs first, then Release, then AUR
# Author: rezky_nightky

name: 🔧 CI Pipeline

on:
  push:
    branches: [main]  # Only main branch (single active branch)
  pull_request:
    branches: [main]
  workflow_dispatch:
  workflow_call:  # Allow other workflows to call this

# Security: Explicit permissions (principle of least privilege)
permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  SCCACHE_GHA_ENABLED: false

jobs:
  # =============================================================================
  # STEP 3: SECURITY & QUALITY CHECKS
  # =============================================================================
  security:
    name: 3️⃣ Security & Quality
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'push' || !startsWith(github.ref, 'refs/heads/') || (!contains(github.event.head_commit.message, '[skip ci]') && !startsWith(github.event.head_commit.message, 'release: ')) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Setup sccache (optional)
        uses: mozilla-actions/sccache-action@v0.0.9
        continue-on-error: true
        with:
          version: "v0.5.4"

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Run code formatting check
        run: cargo fmt --check

      - name: Run Clippy
        run: |
          unset RUSTC_WRAPPER
          cargo clippy --all-targets --all-features -- -D warnings

  # =============================================================================
  # STEP 4: BUILD DEBUG & TEST
  # =============================================================================
  build-debug:
    name: 4️⃣ Build Debug & Test
    runs-on: ubuntu-latest
    needs: security

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Setup sccache (optional)
        uses: mozilla-actions/sccache-action@v0.0.9
        continue-on-error: true
        with:
          version: "v0.5.4"

      - name: Cache Cargo target
        uses: actions/cache@v4
        with:
          path: target/
          key: ${{ runner.os }}-cargo-target-debug-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-target-debug-
            ${{ runner.os }}-cargo-target-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: Build Debug
        run: |
          echo "Building debug version for validation..."
          unset RUSTC_WRAPPER  # Disable sccache if causing issues
          cargo build --target x86_64-unknown-linux-gnu

      - name: Run tests
        run: |
          echo "Running tests with GitHub Actions default cores..."
          unset RUSTC_WRAPPER  # Disable sccache if causing issues
          cargo test --target x86_64-unknown-linux-gnu

  # =============================================================================
  # FUTURE: MACOS BUILD (COMMENTED OUT)
  # =============================================================================
  # build-macos:
  #   name: Build and Test (macOS)
  #   runs-on: macos-latest
  #   needs: quality
  #   if: false  # Disabled for now
  #   strategy:
  #     matrix:
  #       target: [x86_64-apple-darwin, aarch64-apple-darwin]
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5
  #
  #     - name: Setup Rust toolchain
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         targets: ${{ matrix.target }}
  #
  #     - name: Build
  #       run: |
  #         cargo build --release --target ${{ matrix.target }}
  #
  #     - name: Upload artifacts
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: lyvoxa-${{ matrix.target }}
  #         path: target/${{ matrix.target }}/release/lyvoxa*

  # =============================================================================
  # STEP 5: PERFORMANCE MONITORING
  # =============================================================================
  performance:
    name: 5️⃣ Performance Monitoring
    runs-on: ubuntu-latest
    needs: build-debug
    if: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Download debug artifacts
        uses: actions/download-artifact@v5
        with:
          name: lyvoxa-debug-${{ github.sha }}
          path: ./artifacts/

      - name: Run debug validation
        run: |
          chmod +x artifacts/lyvoxa

          echo "📊 Debug Build Analysis:"
          echo "========================="
          ls -lh artifacts/
          echo ""

          echo "📈 Binary Information:"
          echo "====================="
          file artifacts/lyvoxa
          echo ""

          echo "🔍 Binary Dependencies:"
          echo "======================"
          ldd artifacts/lyvoxa || echo "Static binary"
          echo ""

          echo "🧪 Binary Validation:"
          echo "===================="
          if readelf -h artifacts/lyvoxa > /dev/null 2>&1; then
            echo "✅ Debug build is valid ELF binary"
          else
            echo "❌ Debug build is invalid"
            exit 1
          fi

          echo ""
          echo "✅ Debug build validated successfully"

  # =============================================================================
  # STEP 5: BUILD OPTIMIZED RELEASE
  # =============================================================================
  build-release:
    name: 5️⃣ Build Optimized Release
    runs-on: ubuntu-latest
    needs: performance
    if: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Cache Cargo target
        uses: actions/cache@v4
        with:
          path: target/
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-release-
            ${{ runner.os }}-cargo-

      - name: Build Optimized Release
        run: |
          echo "🚀 Building optimized release binary..."
          export RUSTFLAGS="-C opt-level=z -C lto=fat -C codegen-units=1 -C strip=symbols"
          cargo build --release --target x86_64-unknown-linux-gnu

          echo ""
          echo "📊 Optimized Binary Analysis:"
          echo "  Size: $(du -h target/x86_64-unknown-linux-gnu/release/lyvoxa | cut -f1)"
          echo "  Type: $(file target/x86_64-unknown-linux-gnu/release/lyvoxa)"

      - name: Verify release binary
        run: |
          echo "🧪 Verifying release binary..."
          if readelf -h target/x86_64-unknown-linux-gnu/release/lyvoxa > /dev/null 2>&1; then
            echo "✅ Release binary is valid ELF"
          else
            echo "❌ Release binary is invalid"
            exit 1
          fi

          echo "✅ Optimized release build completed successfully"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lyvoxa-release-${{ github.sha }}
          path: target/x86_64-unknown-linux-gnu/release/lyvoxa
          retention-days: 1

# =============================================================================
# WORKFLOW CONFIGURATION
# =============================================================================

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
